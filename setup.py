from setuptools import setup
setup(name='PyShelf',
      version='0.1',
      description='Shelf: Something convenient',
      author='Morgan Stuart',
      packages=['PyShelf'])
      #packages=['common', 'index_descriptions', 'resources', 'util'])
